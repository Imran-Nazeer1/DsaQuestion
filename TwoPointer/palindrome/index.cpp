#include<bits/stdc++.h>
using namespace std;
int main (){
    string s="i am a boy ";
    int size=s.size();
    for(int i=0;i<size;i++){
        cout<<s[i];
        isalnum(s[i]);
    }
    cout<<endl;
    for(int i=0;i<size;i++){
        cout<<s[i];
    }

    // bool yes=isalnum(s);
}